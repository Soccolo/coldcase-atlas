# Optional. Without these the app runs fine on official data alone;
# the community layer simply stays switched off.
#
# Create a "script" app at https://www.reddit.com/prefs/apps
# You need the client id (under the app name) and the secret. No password.
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=
